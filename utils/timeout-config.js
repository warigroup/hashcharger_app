// timeout in milliseconds
export const TIMEOUT_DURATION = 11000;

// toast duration
export const TOAST_DURATION = 4000;

// wait a little bit until alert animation or toast.
export const INIT_ALERT = 500;

// wait for setState change 
export const SELECT_DELAY = 650;

// load data delay
export const LOAD_DELAY = 560;

// avoid initial page load
export const PAGE_LOAD = 50;

// wait for feedback alert
export const WAIT_ALERT = 1300;

// wait until other modal fades out
export const MODAL_FADE = 1900;

// wait until hashrate select function fires
export const WAIT_INIT_SELECT = 350;
export const WAIT_SELECT = 450;

// wait until network error
export const NTSTATUS = 1000;

// wait for a test cookie
export const COOKIE_TIMEOUT = 40;

// preload delay. preload has a lower priority.
export const PRELOAD_DELAY = 1200;

// referral code expiration time in days
export const REFERRAL_CODE_COOKIE_EXPIRATION_DAYS = 7;